<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.administrator.newfridge">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera.front" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-sdk android:minSdkVersion="10"
        tools:ignore="GradleOverrides" />
    <uses-feature android:name="android.hardware.nfc" android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup">

        <activity android:name=".widget.GuideActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".family.FamilyActivity" />
        <activity android:name=".qrcodeshow.QrshowActivity" />
        <activity android:name=".nfc.NfcActivity">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.TECH_DISCOVERED"/>
            </intent-filter>

            <meta-data android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc" />
            <intent-filter>
                <action android:name="android.nfc.action.TAG_DISCOVERED"/>
            </intent-filter>
        </activity>
        <activity android:name=".loginandregiste.LoginActivity" />
        <activity android:name=".loginandregiste.RegisterActivity" />
        <activity android:name=".qrcode.QRScannerActivity" />
        <activity android:name=".me.Meinformation" />
        <activity android:name=".food.FoodActivity" />
        <activity android:name=".foodmenu.FoodMenuActivity" />
        <activity android:name=".me.MeActivity" />
        <activity android:name=".record.RecordActivity" />
        <activity android:name=".foodmenushow.FoodMenuShowActivity" />
        <activity android:name=".fridgemanager.FridgeActivity" />
        <activity android:name=".fridgemanager.FridgeShowActivity" />
        <activity android:name=".foodshow.FoodShowActivity" />
    </application>

</manifest>